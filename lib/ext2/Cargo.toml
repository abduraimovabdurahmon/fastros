[package]
name = "fastros-ext2"
description = "ext2 filesystem implementation (read/write + mkfs) used by the FastROS kernel"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
